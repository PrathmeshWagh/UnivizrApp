import { StyleSheet, Text, View, FlatList } from 'react-native'
import React from 'react'
import { } from 'react-native-gesture-handler'

const MultipleStar = () => {
  return (
    <View>
      <FlatList />
    </View>
  )
}

export default MultipleStar

const styles = StyleSheet.create({})